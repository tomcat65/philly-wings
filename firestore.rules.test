rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Test/Emulator rules - allow all operations for seeding data
    match /{document=**} {
      allow read, write: if true;
    }
  }
}